services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: evassessment-api
    environment:
      DATABASE_URL: sqlite:///./data/addresses.db
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    restart: unless-stopped
